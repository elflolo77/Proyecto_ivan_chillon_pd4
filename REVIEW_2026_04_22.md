# Revisión actualizada del proyecto — Alejandro (Cine Flolix)

**Fuente de verdad:** `proyecto/03-testing/cine_multiplex/` (hay también `04-sqlite/` pero queda fuera de alcance en esta pasada).
**Alcance:** seguimiento de las observaciones "a mejorar" recogidas en [REVIEW_2026_04_10.md](REVIEW_2026_04_10.md) para las Fases **01 y 02**, y revisión de la entrega de la **Fase 03**.
**Fuera de alcance:** Fase 04 (SQLite) se revisará en una iteración posterior.

---

## REVISIÓN FASE 01 - 2026-04-22 — Nota: 10/10

### Aspectos resueltos

- **[DISEÑO] `application/servicios.py` eliminado.** El fichero obsoleto con métodos `NotImplementedError` ya no existe; el código utiliza exclusivamente `application/servicio_cine.py`.

- **[BUG] `application/servicio_cine.py` — Encapsulamiento del repositorio respetado.** `anular_entrada()` ya no accede a `self._repositorio._coleccion_entradas.remove(...)`: ahora llama al método público `eliminar_entrada(identificador_entrada)` del repositorio (`servicio_cine.py:110`). El método está declarado en la interfaz `RepositorioCine` (`domain/repositorio.py:50-52`) e implementado en `RepositorioMemoria` (`infrastructure/repositorio_memoria.py:58-63`).

- **[IMPORTANTE] `anular_entrada()` accesible desde el menú.** El submenú "Venta de Entradas" incluye la opción "2. Anular entrada" (`presentation/menu.py:141-150`). Pide el ID de entrada, llama al servicio y notifica el resultado.

- **[DISEÑO] Validación de solapamiento de sesiones implementada.** La variable `sesiones_sala` ya no está huérfana: se recorren todas las sesiones existentes y se rechaza la programación si ya hay una en la misma sala y fecha/hora (`application/servicio_cine.py:69-71`, `ValueError: "Ya existe una sesión en la sala N a las ..."`).

- **[DISEÑO] `domain/entrada.py` — `_fecha_venta` con valor real.** Ahora se asigna `datetime.now()` en el constructor (`domain/entrada.py:15`) en lugar de quedar fijado a `None`.

- **[DISEÑO] Validación de películas duplicadas.** Los tres métodos `registrar_pelicula_comercial/infantil/clasica` consultan primero `repositorio.obtener_pelicula_por_titulo(titulo)` y lanzan `ValueError("La película '...' ya existe.")` si aparece (`application/servicio_cine.py:17, 25, 33`).

- **[IMPORTANTE] `cine_multiplex/cine_db.json` eliminado.** El fichero obsoleto ya no está en el proyecto ni hay referencias a él desde el código Python.

### Sigue cumpliendo

- Arquitectura por capas correcta, contrato de repositorio en `domain/`, herencia polimórfica en `Pelicula`, encapsulamiento con `@property`.
- Entradas numéricas del menú dentro de `try/except` (bugfix previo).

### Observaciones pendientes

- Sin observaciones nuevas para la Fase 01.

## REVISIÓN FASE 02 - 2026-04-22 — Nota: 9/10

### Aspectos resueltos

- **[CRÍTICO] Documentación alineada con el código.** Las inconsistencias señaladas ya están resueltas:
  - `REGLAS_DE_NEGOCIO.md` reescrito; ya no usa `capacidad_disponible` ni describe funcionalidades no implementadas.
  - `CASOS_DE_USO.md:34-42` describe ahora "Anular Entrada" como opción real, reflejando la opción 2 del submenú de ventas.
  - `ARQUITECTURA_POR_CAPAS.md:4` habla de "Arquitectura por Capas (N-Tier Architecture)" en lugar de "Onion Architecture".

- **[IMPORTANTE] Comentarios forzados eliminados.** `domain/pelicula.py` ya no arrastra el comentario `# Memoria: minutos (int > 0)` ni la terminología extraña; los docstrings son concisos y centrados en el propósito.

- **[IMPORTANTE] Proyecto modelo `cepy_pd4` retirado** de `02-documentando/`. La carpeta ya no está.

### Sigue cumpliendo

- Carpeta `docs/` completa (`DESCRIPCION_Y_ALCANCE`, `EJECUCION`, `ARQUITECTURA_POR_CAPAS`, `CASOS_DE_USO`, `REGLAS_DE_NEGOCIO`, `MODELO_DE_DOMINIO`, `CONTRATO_REPOSITORIO`, `DATOS_INICIALES`, `TESTS_Y_PASOS`, `TROUBLESHOOTING` y `README.md` como índice).
- Docstrings en módulos, clases y métodos públicos.
- Nombres PEP8, identificadores descriptivos.
- CHANGELOG.md con versión 0.2.0 correctamente redactada.

### Observaciones pendientes

- **Inconsistencia al revés en `REGLAS_DE_NEGOCIO.md`.** El código **ahora sí** valida solapamiento de sesiones (`servicio_cine.py:69-71`) y películas duplicadas (`servicio_cine.py:17, 25, 33`), pero el documento no las recoge. Añade dos reglas (una en "Reglas de Aplicación: Programación de sesiones" y otra en "Registro de películas") describiendo el comportamiento real, por simetría con el resto del documento.

- **Resto de plantilla en `CHANGELOG.md`.** La entrada `[0.1.0]` sigue diciendo "Aplicación base de **máquina expendedora** por capas"; el resto del punto también alude a "items, descuentos, compra/cancelación, stock" (vocabulario de la expendedora, no del cine). Reescríbelo con términos del dominio real (películas, salas, sesiones, venta/anulación de entradas).

## REVISIÓN FASE 03 - 2026-04-22 — Nota: 9,5/10

Primera entrega de Fase 03. Se cumplen todos los puntos de la checklist oficial. Quedan un par de observaciones menores sobre el reporte de cobertura y el estilo del documento.

### Cumple (según checklist oficial)

- **Tests para ≥ 2 clases del dominio**: `Pelicula` (y sus subclases) y `Sala` — requisito mínimo satisfecho.
  - `tests/test_pelicula.py` (7 tests sobre `PeliculaComercial/Infantil/Clasica`).
  - `tests/test_sala.py` (3 tests sobre `Sala`).
- **Todos los tests pasan con `python -m unittest`** → `python -m unittest discover cine_multiplex/tests` devuelve **10 tests OK**.
- **`coverage` añadido como dependencia de fase** en `requirements.txt`.
- **Ejecución de cobertura documentada**: `docs/TESTS_Y_PASOS.md` describe los comandos `coverage run` y `coverage report` con tabla de resultados.
- **`docs/EJECUCION.md` con pasos desde clonado hasta ejecución**: sección "ACTUALIZACIÓN - 2026-04-15 (Fase 03: Testing)" con instalación de `requirements.txt`, ejecución del `main.py`, ejecución de los tests y generación del reporte de cobertura.
- **Entrada `[0.3.0]` en `CHANGELOG.md`** correctamente redactada.
- Paquete `cine_multiplex/tests/` con `__init__.py` y tests organizados.

### Errores y aspectos a mejorar

- **[DISEÑO] Estilo en `TESTS_Y_PASOS.md`.** El apartado "Cómo probar la aplicación a mano (Legacy)" (líneas 36-46) usa el símbolo `$` (dólar) en los importes ("5.00$") y arrastra texto poco coherente con el dominio cine. Cámbialo a `€` y revisa que los pasos (ID `S1`, película `"Dune: Parte Dos"`, etc.) coincidan con los IDs y películas reales de `datos_iniciales.py`.


