# Revisión del proyecto — Alejandro (Cine Flolix)

**Fuente de verdad:** `proyecto/02-documentando/cine_multiplex/`
**Fases detectadas:** 02 (documentación) — código en `proyecto/02-documentando/` sin carpetas numeradas dentro del proyecto

## REVISIÓN FASE 02 - 2026-04-10 — Nota: 8/10

### ✅ Resuelto desde la revisión anterior (2026-03-03)

- ✓ **Carpeta `docs/` creada** con todos los documentos requeridos: DESCRIPCION_Y_ALCANCE.md, EJECUCION.md, ARQUITECTURA_POR_CAPAS.md, CASOS_DE_USO.md, REGLAS_DE_NEGOCIO.md, MODELO_DE_DOMINIO.md, CONTRATO_REPOSITORIO.md, DATOS_INICIALES.md, TESTS_Y_PASOS.md, TROUBLESHOOTING.md.
- ✓ **CHANGELOG.md creado** en la raíz con versión 0.2.0.
- ✓ **Docstrings de métodos agregados** a menú, servicios, clases de dominio. Ahora todos los métodos públicos tienen docstrings.
- ✓ **Reglas de negocio comentadas** en las clases del dominio (sesion.py, pelicula.py, etc.).
- ✓ **README.md actualizado** con instrucciones relativas correctas, descripción del proyecto y referencias a la documentación.
- ✓ **Variables renombradas** en `datos_iniciales.py`: ahora son `pelicula_dune`, `pelicula_kung_fu`, `sala_imax`, etc. (en lugar de `p1`, `p2`, `s1`).
- ✓ **Ficheros huérfanos eliminados** de la raíz (aunque `cine_db.json` aún existe en cine_multiplex/).
- ✓ **Nombres corregidos** en presentación: `duracion_minutos` en lugar de `duracion`, `opcion_seleccionada` en lugar de `op`, `clasificacion` en lugar de `clasif`.

### Cumple

- Código organizado en carpeta `02-documentando` con estructura de capas clara: `domain/`, `application/`, `infrastructure/`, `presentation/`.
- Docstrings presentes en módulos (primera línea antes de imports), en todas las clases públicas y en todos los métodos públicos.
- Carpeta `docs/` completa con 11 documentos bien estructurados siguiendo el modelo.
- README.md en raíz y en `02-documentando/` con instrucciones claras.
- CHANGELOG.md presente con versión 0.2.0 y cambios documentados.
- Reglas de negocio explícitamente comentadas en el dominio.
- Nombres de identificadores conformes a PEP8 (snake_case para variables, PascalCase para clases).
- Propiedades (`@property`) usadas correctamente para encapsulamiento.
- Buena separación de capas: servicios orquestando use cases, dominio con lógica de negocio, infraestructura con repositorio en memoria.

### Errores y aspectos a mejorar

- **[IMPORTANTE] `cine_multiplex/cine_db.json` aún presente.** Fue el objetivo eliminarlo en la revisión anterior. Este fichero no se usa y contamina el repositorio.
  - *Cómo resolverlo:* Elimina el fichero y confirma que no hay referencias a él en el código Python.

- **[BUG] `application/servicio_cine.py:anular_entrada()` aún intenta llamar a `self.repo.guardar_datos()` que no existe.** Esto provoca `AttributeError` si se invoca la función. Aunque no es accesible desde el menú, la función tiene un defecto de implementación.
  - *Cómo resolverlo:* Implementa correctamente `eliminar_entrada(id)` en `RepositorioMemoria` (y decláralo en la interfaz `RepositorioCine`). Úsalo en lugar de `self.repo.entradas.remove()` y elimina la llamada a `guardar_datos()`.

- **[BUG] No hay validación de películas duplicadas.** Es posible registrar la misma película (mismo título) varias veces sin que el sistema lo detecte, generando inconsistencias en búsquedas.
  - *Cómo resolverlo:* Antes de guardar una película en cualquier método `registrar_pelicula_*()`, verifica: `if self.repo.obtener_pelicula(titulo): raise ValueError("Película ya existe")`.

- **[DISEÑO] `application/servicio_cine.py:59` — La variable `sesiones_sala` se calcula pero nunca se usa.** La validación de solapamiento de sesiones no está implementada. Puede programarse dos películas en la misma sala a la misma hora.
  - *Cómo resolverlo:* Usa `sesiones_sala` para comprobar solapamientos en horario y lanza `ValueError` si hay conflicto. O elimina la variable si no será implementado en esta fase.

- **[DISEÑO] `domain/entrada.py:14` — `self._fecha_venta = None` nunca se asigna.** El atributo siempre devolverá `None`. Aunque hay un comentario "Reservado para uso futuro", debería asignarse al constructor o eliminarse.
  - *Cómo resolverlo:* Asigna la fecha actual al construir la entrada: `from datetime import datetime; self._fecha_venta = datetime.now()`. O elimina el atributo si no se usa aún.

- **[IMPORTANTE] Fase 01 aún incompleta en el repositorio.** El checklist del README.md (líneas 29-48) incluye marcas `[x]` para criterios de fase 01, pero no hay evidencia clara de una carpeta `01-diseno_capas/` al nivel superior del proyecto (solo está dentro de `cepy_pd4/` que es el modelo copiado).
  - *Cómo resolverlo:* Estructura el repositorio con carpetas explícitas `proyecto/01-diseno_capas/` y `proyecto/02-documentando/` si fase 01 es requisito de entrega. Actualmente solo se entrega fase 02.

---

## REVISIÓN FASE 01 - 2026-04-10 — Nota: 8,5/10

### ✅ Resuelto desde la revisión anterior (2026-03-03)

- ✓ **Bugs de presentación corregidos**: `duracion_minutos` y `numero_sala` ahora se convierten dentro de bloques `try/except` (líneas 65 y correspondiente en sesiones).
- ✓ **Nombres de variables mejorados**: `duracion` → `duracion_minutos`, `op` → `opcion_seleccionada`, `clasif` → `clasificacion`.
- ✓ **Métodos de menú renombrados y documentados**: `mostrar_menu_peliculas()`, `mostrar_menu_salas()`, etc.
- ✓ **Docstrings agregados** a todas las clases y métodos del menú.

### Cumple

- Repositorio con estructura clara: `proyecto/` contiene código organizando en capas.
- Arquitectura en capas correctamente implementada: `domain/` (entidades Pelicula, Sala, Sesion, Entrada), `application/` (ServicioCine), `infrastructure/` (RepositorioMemoria, datos_iniciales), `presentation/` (MenuCine).
- POO aplicado correctamente: herencia (PeliculaComercial, PeliculaInfantil, PeliculaClasica heredan de Pelicula), encapsulamiento con `@property`, contrato de repositorio en domain/repositorio.py.
- Estructura de ficheros con módulos y paquetes bien organizados.
- Nombres significativos y conformes a PEP8 (snake_case para variables, métodos; PascalCase para clases).
- Menú principal con 6 opciones funcionando correctamente: Gestión de Películas, Salas, Sesiones, Ventas, Informes, Salir.
- Flujos principales funcionan: registrar películas, crear salas, programar sesiones, vender entradas, mostrar estadísticas.

### Errores y aspectos a mejorar

- **[DISEÑO] `application/servicios.py` aún presente pero huérfano.** Contiene una clase `ServicioCine` alternativa sin uso. Aunque el código actual usa `servicio_cine.py`, esta duplicación contamina el repositorio.
  - *Cómo resolverlo:* Verifica que no haya importaciones a `servicios.py` y elimina el fichero completamente.

- **[BUG] `application/servicio_cine.py:anular_entrada()` — Intenta acceder a `self.repo.entradas.remove()` (línea 115).** Esto rompe el encapsulamiento del repositorio (acceso directo a datos privados). Si se invoca, puede dejar inconsistencias.
  - *Cómo resolverlo:* Añade método público `eliminar_entrada(id)` en `RepositorioMemoria` y úsalo desde el servicio.

- **[BUG] `application/servicio_cine.py:anular_entrada()` — Llama a `self.repo.guardar_datos()` (línea 117) que no existe.** Causa `AttributeError` si se invoca. Aunque no es accesible desde el menú, la función tiene defecto crítico.
  - *Cómo resolverlo:* Elimina la llamada a `guardar_datos()` (no existe en RepositorioMemoria).

- **[IMPORTANTE] `anular_entrada()` nunca se accede desde el menú.** Es funcionalidad huérfana. Si es requisito, añade opción en menú de ventas. Si no, documenta esta limitación en TROUBLESHOOTING.md.
  - *Cómo resolverlo:* Decide si es requisito. Si sí, añade opción al menú. Si no, elimina la función y documenta por qué en TROUBLESHOOTING.md.

- **[DISEÑO] `application/servicio_cine.py:59` — Variable `sesiones_sala` calculada pero nunca usada.** No hay validación de solapamiento de sesiones. Puede programarse dos películas en misma sala/hora.
  - *Cómo resolverlo:* Implementa la validación usando `sesiones_sala` o elimina la variable si no es requisito.

- **[DISEÑO] `domain/entrada.py:14` — `self._fecha_venta = None` nunca se asigna a fecha real.** Atributo siempre devuelve `None`.
  - *Cómo resolverlo:* Asigna fecha actual al construir: `self._fecha_venta = datetime.now()`, o elimina si no se usa.

- **[DISEÑO] No hay validación de películas duplicadas.** Se puede registrar misma película varias veces.
  - *Cómo resolverlo:* Añade validación antes de guardar: `if self.repo.obtener_pelicula(titulo): raise ValueError(...)`.

---

## REVISIÓN FASE 03 - 2026-04-10 — Nota: 0/10

> No entregado. No hay carpeta `03-testing/` ni fichero `requirements.txt` en `proyecto/02-documentando/`. Los tests unitarios no están implementados aún para Cine Flolix. (Nota: Existe una copia del modelo `cepy_pd4/03-testing/` con tests del proyecto expendedora, pero no del proyecto Cine Flolix del alumno).

