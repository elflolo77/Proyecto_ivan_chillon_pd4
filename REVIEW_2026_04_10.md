# Revisión del proyecto — Alejandro (Cine Flolix)

**Fuente de verdad:** `proyecto/02-documentando/cine_multiplex/`
**Fases detectadas:** 02 (documentación) — código en `proyecto/02-documentando/` sin carpetas numeradas dentro del proyecto

## REVISIÓN FASE 01 - 2026-04-10 — Nota: 7,5/10

### Resuelto desde la revisión anterior (2026-03-03)

- Bugs de entrada sin try/except corregidos: `duracion_minutos = int(input(...))` (línea 65) ahora está dentro del bloque `try/except` en `mostrar_menu_peliculas()`. Igual con `numero_sala` en `mostrar_menu_sesiones()`.

### Cumple

- Repositorio con estructura clara: `proyecto/` contiene código organizado en capas.
- Arquitectura en capas correctamente implementada: `domain/` (Pelicula, Sala, Sesion, Entrada), `application/` (ServicioCine), `infrastructure/` (RepositorioMemoria, datos_iniciales), `presentation/` (MenuCine).
- POO aplicado correctamente: herencia (PeliculaComercial, PeliculaInfantil, PeliculaClasica heredan de Pelicula), encapsulamiento con `@property`, contrato de repositorio en domain/repositorio.py.
- Estructura de ficheros con módulos y paquetes bien organizados, nombres significativos conformes a PEP8.
- Menú principal con 6 opciones funcionando: Gestión de Películas, Salas, Sesiones, Ventas, Informe de Ventas, Salir.
- Flujos principales funcionan correctamente: registrar películas, crear salas, programar sesiones, vender entradas, mostrar estadísticas.
- Entradas numéricas en menú ahora validadas dentro de bloques try/except (bugfix importante).

### Errores y aspectos a mejorar

- **[DISEÑO] `application/servicios.py` existe pero no se usa.** Tienes una clase `ServicioCine` antigua con métodos `listar_todas_las_peliculas()`, `buscar_pelicula_por_titulo()`, `buscar_sala_por_numero()` y otros no implementados que lanzan `NotImplementedError`. Tu código usa correctamente `servicio_cine.py` que tiene la implementación actualizada. No hay importaciones a `servicios.py` en el proyecto.
  - *Cómo resolverlo:* Elimina el fichero `servicios.py` completamente.

- **[BUG] `application/servicio_cine.py:115` — En `anular_entrada()` accedes directamente a `self._repositorio._coleccion_entradas.remove()`.** Estás rompiendo el encapsulamiento del repositorio accediendo a su atributo privado `_coleccion_entradas`. Si cambias la estructura interna del repositorio, esta función se rompe.
  - *Cómo resolverlo:* Implementa un método público `eliminar_entrada(id)` en `RepositorioMemoria` (decláralo también en la interfaz `RepositorioCine`) y úsalo en lugar del acceso directo.

- **[IMPORTANTE] `anular_entrada()` no es accesible desde el menú.** Has implementado la función pero no la has conectado a ninguna opción del menú, por lo que el usuario no puede usarla.
  - *Cómo resolverlo:* Decide si es requisito. Si sí, añade la opción al menú. Si no, elimina la función y documenta la limitación en TROUBLESHOOTING.md.

- **[DISEÑO] `application/servicio_cine.py:59` — Calculas la variable `sesiones_sala` pero nunca la usas.** No estás validando que pueda haber solapamiento de sesiones: puedes programar dos películas en la misma sala a la misma hora sin que el sistema lo detecte.
  - *Cómo resolverlo:* Usa `sesiones_sala` para implementar la validación de solapamiento, o elimina la variable si no es requisito de esta fase.

- **[DISEÑO] `domain/entrada.py:14` — Declaras `self._fecha_venta = None` pero nunca le asignas un valor real.** El atributo siempre devolverá `None`.
  - *Cómo resolverlo:* Asígnale la fecha actual en el constructor: `self._fecha_venta = datetime.now()`, o elimínalo si no lo vas a usar.

- **[DISEÑO] No validas películas duplicadas.** Puedes registrar la misma película varias veces sin que el sistema lo detecte.
  - *Cómo resolverlo:* Añade validación en los métodos `registrar_pelicula_*()`: antes de guardar, comprueba si ya existe una película con ese título.

- **[IMPORTANTE] `cine_multiplex/cine_db.json` aún presente.** Es un fichero que no usas y que ya se indicó en la revisión anterior que debías eliminar.
  - *Cómo resolverlo:* Elimínalo y verifica que no haya referencias a él en el código Python.


## REVISIÓN FASE 02 - 2026-04-10 — Nota: 7/10

La estructura formal de la fase está completa (todos los documentos presentes, docstrings, nombres renombrados), pero la documentación en varios ficheros clave no describe el proyecto real sino un proyecto ideal que no has implementado. Eso va directamente contra el objetivo de esta fase. Además, hay errores indicados en la revisión anterior que siguen sin corregirse.

### Resuelto desde la revisión anterior (2026-03-03)

- Carpeta `docs/` creada con todos los documentos requeridos: DESCRIPCION_Y_ALCANCE.md, EJECUCION.md, ARQUITECTURA_POR_CAPAS.md, CASOS_DE_USO.md, REGLAS_DE_NEGOCIO.md, MODELO_DE_DOMINIO.md, CONTRATO_REPOSITORIO.md, DATOS_INICIALES.md, TESTS_Y_PASOS.md, TROUBLESHOOTING.md.
- CHANGELOG.md creado en la raíz con versión 0.2.0.
- Nombres de variables renombrados para cumplir criterios de nomenclatura: `p1, p2, p3` → `pelicula_dune, pelicula_kung_fu, pelicula_clasica`; `s1, s2, s3` → nombres descriptivos; `duracion` → `duracion_minutos`, `op` → `opcion_seleccionada`, `clasif` → `clasificacion`.
- Métodos de menú renombrados y documentados: `mostrar_menu_peliculas()`, `mostrar_menu_salas()`, `mostrar_menu_sesiones()`, `mostrar_menu_ventas()`, `mostrar_menu_estadisticas()`.
- Docstrings agregados a todos los módulos (primera línea antes de imports), todas las clases públicas y todos los métodos públicos.
- Reglas de negocio comentadas explícitamente en las clases del dominio con comentarios `# Regla de negocio:...` junto a su implementación.
- README.md actualizado en raíz y en `02-documentando/` con instrucciones relativas correctas, descripción clara del proyecto y referencias a documentación en `docs/`.

### Cumple

- Carpeta `docs/` completa con 11 documentos bien estructurados siguiendo el modelo (README.md en docs/ como índice).
- Docstrings presentes en módulos (primera línea antes de imports), en todas las clases y en todos los métodos públicos.
- CHANGELOG.md en raíz del repositorio con versión 0.2.0 y cambios documentados.
- Nombres de identificadores conformes a PEP8: snake_case para variables y métodos, PascalCase para clases.
- Reglas de negocio explícitamente comentadas junto a su implementación en el dominio.
- Estructura de capas clara y bien documentada: `domain/`, `application/`, `infrastructure/`, `presentation/`.
- Propiedades (`@property`) usadas correctamente para encapsulamiento de atributos.
- Separación clara de responsabilidades: servicios orquestan use cases, dominio contiene lógica de negocio, infraestructura maneja persistencia.

### Errores y aspectos a mejorar

- **[CRÍTICO] Tu documentación no coincide con el código que tienes implementado.** En varios documentos describes funcionalidades o nombres que no existen en el código real:
  - `REGLAS_DE_NEGOCIO.md` línea 7: usas `capacidad_disponible` pero en el código la propiedad se llama `numero_asientos_libres`
  - `REGLAS_DE_NEGOCIO.md` líneas 13-14: describes una validación de solapamiento de sesiones que NO has implementado
  - `REGLAS_DE_NEGOCIO.md` líneas 21-22: describes una validación de películas duplicadas que NO has implementado
  - `CASOS_DE_USO.md` líneas 40-43: describes "Anular Entrada" como opción accesible desde el menú cuando NO existe esa opción
  - `ARQUITECTURA_POR_CAPAS.md` línea 4: mencionas "Onion Architecture" siendo una arquitectura simple en capas
  - *Cómo resolverlo:* Después de implementar el código, revisa todos los documentos y verifica que describen exactamente lo que tienes, no lo ideal. Elimina de la documentación las funcionalidades que no has implementado.

- **[IMPORTANTE] Tienes comentarios forzados en el código que no aportan valor:**
  - `domain/pelicula.py:8`: el comentario `# Memoria: minutos (int > 0)` es innecesario y usa un lenguaje extraño
  - Varios comentarios en `domain/` usan terminología técnica innecesaria
  - En `REGLAS_DE_NEGOCIO.md` escribes frases como "Para uso productivo mayor, el módulo datetime vigilará..." que no corresponden a nada real
  - *Cómo resolverlo:* Elimina los comentarios que no expliquen el **por qué** del código. Mantén solo los que expliquen decisiones de diseño o restricciones del negocio.

- **[IMPORTANTE] Tu repositorio tiene el proyecto modelo de la expendedora en la subcarpeta `02-documentando`**
  - *Cómo resolverlo:* Elimina la carpeta `cepy_pd4` o muévela a la carpeta padre

## REVISIÓN FASE 03 - 2026-04-10 — Nota: 0/10

Sin entregar.


## ANÁLISIS: INDICIOS DE USO ACRÍTICO DE IA

Se detectan evidencias de que utilizas la IA de forma **acrítica**, copiando y pegando contenido sin reflexión propia ni verificación. La IA es herramienta de aprendizaje, no sustituto de pensamiento propio/crítico. Si realmente quieres aprender no deberías abusar/dar por bueno lo que la IA genera de forma acrítica. Revisa trata de entender y reescribe con tus palabras. 

Algunas posibles evidencias de uso acrítico:

- Inconsistencias código-documentación
- Propiedades sin usar (código que luego no se utiliza)
- Lenguaje no personalizado, fórmulas genéricas de IA
- Documentación que describe lo "ideal" vs. lo "real"
- Uso de tipado en código  (-> str, -> int, -> bool, etc.) cuando no lo hemos visto en clase ni lo hemos utilizado.

**Recomendación:** La IA, bien usada es una herramienta potente de aprendizaje. Esta bien usar IA como punto de partida o para que te explique o proponga mejoras, pero siempre deberías:
1. Verificar que lo generado coincida con tu código real
2. Editar para personalizar y eliminar lenguaje forzado
3. Limpiar código que no uses
4. Preguntar al código: ¿realmente necesito esto?
5. Etc.

